replicaCount: 1

image:
  repository: betashow/video-generation-api   # public image, no login needed
  tag: latest

service:
  type: ClusterIP
  port: 5000          # matches the container port of the public image

containerPort: 5000  # matches the image default
